name: portainer
image: portainer/portainer-ce:latest
command:
  - -H tcp://0.0.0.0:2375
environment:
  - ADMIN_PASSWORD=poorimontrashcan
  - TZ=America/Vancouver
volumes:
  - name: portainer-data
    host:
      path: /data/portainer
sync:
  - .:/app
forward:
  - 9000:9000
