name: portainer
image: portainer/portainer-ce:latest
command: []
volumes:
  - name: docker-socket
    path: /var/run/docker.sock
  - name: portainer-data
    path: /data
environment:
  ADMIN_PASSWORD: poorimontrashcan

sync:
  - source: ./
    target: /app
