name: portainer
image: portainer/portainer
command:
  - "-H"
  - "unix:///var/run/docker.sock"
forward:
  - "8080:9000"
metadata:
  annotations:
    "dev.okteto.com/auto-create-service": "true"
frontend:
  extraEnv:
    - name: portainer
      value: ".cloud.okteto.net"
