name: portainer
image: portainer/portainer-ce:latest
command: ["/portainer", "--no-auth"]
sync:
  - /var/run/docker.sock:/var/run/docker.sock
forward:
  - 9443:9443
volumes:
  - name: data
    hostPath:
      path: /data
  - /var/run/docker.sock:/var/run/docker.sock
